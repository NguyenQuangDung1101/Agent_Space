Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\agentspace\Scripts\Activate.ps1

ollama signin
python -m share.agent 
python -m uvicorn agent.agent_of_requirement.server:app --port 8001
python -m uvicorn agent.analyze_agent.server:app --port 8002
python -m uvicorn agent.planner_agent.server:app --port 8003
python -m uvicorn agent.synthesizer_agent.server:app --port 8004

curl -X POST "http://127.0.0.1:8002/run" ^
  -H "Content-Type: application/json" ^
  -d "{\"session_id\":\"test-session-1\",\"caller_id\":\"manager\",\"task\":\"Create a Python program that reads a CSV file and calculates average student scores.\",\"context\":{},\"assigned_tool_ids\":[],\"max_steps\":5}"






# CURRENT STATE
    Single/team execution, session persistence, SSE events, and chat GUI are implemented.
    Each user message creates an independent session.
    Conversation memory, pause/resume user contact, Knowledge agents, and Neo4j persistence are not implemented.

# NEXT IMPLEMENTATION STEPS

1. Add Conversation layer
    ├── Create Conversation, Message, and ConversationMemory schemas
    ├── Store sessions inside their parent conversation
    ├── Add conversation and message APIs
    └── Load shared conversation memory into new sessions

2. Add user-contact pause/resume
    ├── Add UserContactRequest schema and WAITING_FOR_USER status
    ├── Allow agents to raise free-text questions for the user
    ├── Save a resumable agent/session checkpoint
    └── Resume the same session after the user replies

3. Update Manager and Orchestrator
    ├── Distinguish new task messages from user-contact replies
    ├── Pause the whole team while waiting for user input
    ├── Resume the requesting agent with the user's response
    └── Update conversation memory after session completion

4. Add Knowledge Graph Modification Agent
    ├── Analyze documents and create an ModificationPlan (ImportPlan/DeletePlan)
    ├── Request user review before importing or deleting
    ├── Import or delete approved data using private built-in tools
    ├── Return validation results and failures
    └── Only work and modify on area connect to Knowledge entity (can not delete this big Knowledge node)

5. Add Neo4j Traversal Agent
    ├── Receive request
    ├── Perform traversal technique using tools (standard is to use get_anchor_node to get related node, then use other tool to explore)
    ├── Retrieve data (nodes and relationships) and return relevant response
    ├── Use only private Neo4j traversal tools
    └── Only work on area connect to Knowledge entity

6. Add Neo4j integration
    ├── Reset and rebuild Agent Space data when app.py starts
    ├── Synchronize registered agents and shared tools
    ├── Store conversations, sessions, plans, executions, and contacts
    ├── Keep the external Knowledge graph as a separate graph domain
    └── Keep file storage as the audit log

7. Update GUI and execution events
    ├── Create and select conversations
    ├── Display connected chat history
    ├── Display user-contact questions and accept free-text replies
    └── Show paused, resumed, completed, and failed session states

8. Validate the complete flow
    ├── Multi-session conversation memory
    ├── Single/team pause and resume
    ├── Knowledge modification approval
    ├── Knowledge graph retrieval
    └── File and Neo4j persistence consistency
```


# UPDATED AGENT SPACE REQUIREMENT

## 1. Core entities

```text
Conversation
    ├── Contains multiple user/assistant messages
    ├── Contains multiple execution Sessions
    ├── Stores shared conversation memory
    └── Must be created before sending tasks

Session
    ├── Represents one user task execution
    ├── Belongs to exactly one Conversation
    ├── Contains analysis, plan, executions, events, and final response
    └── May pause and resume when waiting for user input

Message
    ├── Belongs to a Conversation
    ├── May trigger a new Session
    ├── May answer a pending UserContactRequest
    └── May contain document attachments

ConversationMemory
    ├── Stores a compact summary of previous sessions
    ├── Includes important user requests, decisions, plans, and responses
    └── Is provided as context to new sessions
```

A normal user message creates a new Session.

A message answering a pending user-contact request resumes the existing Session instead of creating another Session.

---

## 2. Project structure

```text
AGENT_SPACE/
│
├── app.py
│   # FastAPI entry point for conversations, messages, sessions,
│   # attachments, events, and GUI.
│
├── requirements.txt
│
├── gui/
│   └── index.html
│       # Conversation-based chat GUI with session status,
│       # agent assignments, user-contact requests, and final responses.
│
├── agent/
│   ├── agent.json
│   │   # Registry for control agents and selectable worker agents.
│   │
│   ├── analyze_agent/
│   │   ├── service.py
│   │   ├── server.py
│   │   ├── system_prompt.txt
│   │   └── builtin_tool.py
│   │
│   ├── planner_agent/
│   │   ├── service.py
│   │   ├── server.py
│   │   ├── system_prompt.txt
│   │   └── builtin_tool.py
│   │
│   ├── agent_of_requirement/
│   │   ├── service.py
│   │   ├── server.py
│   │   ├── base_prompt.txt
│   │   └── builtin_tool.py
│   │
│   ├── synthesizer_agent/
│   │   ├── service.py
│   │   ├── server.py
│   │   ├── system_prompt.txt
│   │   └── builtin_tool.py
│   │
│   ├── knowledge_graph_modification_agent/
│   │   ├── service.py
│   │   │   # Analyzes documents, creates an import plan or delete plan,
│   │   │   requests user review, and imports or deletes approved data.
│   │   ├── server.py
│   │   ├── system_prompt.txt
│   │   └── builtin_tool.py
│   │       # Private document parsing, normalization, validation,
│   │       # node_create (with id, name, description,...)
│   │       # relationship_create (between source and target nodes)
│   │       # node_delete (delete node and related relationship)
│   │       # relationship_delete (delete relationship)
│   │
│   └── neo4j_traversal_agent/
│       ├── service.py
│       │   # Retrieves external knowledge from Neo4j.
│       ├── server.py
│       ├── system_prompt.txt
│       └── builtin_tool.py
│           # Private tools
│           # Return node with id, name, description, property[other attributes]
│           # get_anchor_node (input is a query string, using embeding similarity + lexical (weight sum) with each node (concatenate node attributes) to get topk related nodes (Return [list nodes] + [list rel between nodes in [list nodes]]))
│           # k_hop_expansion (return top k nodes with hop level (Return [list nodes] + [list rel between nodes in [list nodes]]))
│           # path_finding (return top k shortest path (nodes and relationships (not matter direction of rel)) from node a to node b (input))
│
├── tool/
│   ├── tool.json
│   └── get_current_datetime/
│       ├── config.json
│       └── tool.py
│
├── share/
│   ├── local_llm.py
│   ├── agent.py
│   │   # Generic runtime supporting tools, messages, final answers,
│   │   # user-contact requests, pause, and resume.
│   ├── schemas.py
│   │   # Conversation, Message, Session, UserContactRequest,
│   │   # AnalysisResult, plans, executions, and events.
│   ├── registry.py
│   ├── tool_loader.py
│   ├── agent_factory.py
│   ├── event_broker.py
│   └── conversation_store.py
│       # Saves conversation messages, memory, attachments,
│       # session artifacts, and resumable checkpoints.
│
├── core/
│   ├── manager.py
│   │   # Controls conversation sessions, execution branches,
│   │   # user-contact pause/resume, and persistence.
│   └── orchestrator.py
│       # Executes teams, routes agent messages, manages shared context,
│       # and pauses the session when a worker contacts the user.
│
├── database/
│   ├── neo4j_repository.py
│   │   # Main Agent Space graph persistence.
│   ├── knowledge_repository.py
│   │   # Low-level access to the separate external Knowledge graph.
│   ├── schema.cypher
│   │   # Main Agent Space constraints and indexes.
│   └── knowledge_schema.cypher
│       # External Knowledge graph constraints and indexes.
│
└── data/
    └── conversations/
        └── {conversation_id}/
            ├── conversation.json
            ├── messages.json
            ├── memory.json
            ├── attachments/
            └── sessions/
                └── {session_id}/
                    ├── request.json
                    ├── analysis.json
                    ├── single_plan.json or team_plan.json
                    ├── assignments.json
                    ├── events.json
                    ├── outputs.json
                    ├── messages.json
                    ├── tool_results.json
                    ├── failures.json
                    ├── checkpoint.json
                    ├── final_response.json
                    └── session.json
```

The two Knowledge agents are selectable specialist workers.

Their built-in tools are private and are not registered in `tool/tool.json` or assigned by Planner.

---

## 3. Conversation and session flow

```text
Create Conversation
    ↓
User sends message
    ↓
Manager saves message to Conversation
    ↓
If message answers a pending UserContactRequest:
    Resume the existing Session
Else:
    Create a new Session
    ↓
Load ConversationMemory
    ↓
Analyze Agent
    ├── Single execution
    └── Team execution
    ↓
Execute selected solution
    ↓
Save final response as an assistant message
    ↓
Update ConversationMemory
```

The detailed analysis, team formation, tool results, and execution events stay inside the Session.

Conversation memory stores only the useful shared summary needed by later tasks.

---

## 4. User-contact flow

This is a method for every agent to interact with user in mid-task
User contact must be separate from direct or broadcast team communication because it pauses execution.

An agent may emit:

```text
<user_contact>
{
  "question": "Please review the proposed import mapping.",
  "reason": "Confirmation is required before modifying the Knowledge graph.",
  "expected_response": "Free-text review or approval"
}
</user_contact>
```

Flow:

```text
Agent raises UserContactRequest
    ↓
Orchestrator sends it to Manager
    ↓
Session status becomes WAITING_FOR_USER
    ↓
Save active agent state and checkpoint
    ↓
Emit user_contact_requested event
    ↓
GUI displays the agent's question
    ↓
User sends a free-text response
    ↓
Response is linked to the contact request
    ↓
Session status returns to RUNNING
    ↓
Resume the same agent instance and task
```

The user may respond naturally, for example:

```text
The plan is acceptable, but merge duplicate company nodes before importing.
```

The agent may continue, revise its plan, or raise another user-contact request.

For team execution, the whole Session pauses while waiting for the user.

---

## 5. Graph data model

Two graph domains exist in the same Neo4j database.

### Agent Space domain

```text
(System)-[:REGISTERS]->(Agent)
(System)-[:REGISTERS]->(Tool)

(Conversation)-[:HAS_MESSAGE]->(Message)
(Conversation)-[:HAS_SESSION]->(Session)
(Conversation)-[:HAS_MEMORY]->(MemorySnapshot)

(Session)-[:TRIGGERED_BY]->(Message)
(Session)-[:HAS_TASK]->(Task)
(Session)-[:HAS_ANALYSIS]->(Analysis)
(Task)-[:SOLVED_BY]->(Solution)

(Solution)-[:USES_AGENT]->(Agent)
(Solution)-[:USES_TOOL]->(Tool)

(Session)-[:HAS_EXECUTION]->(Execution)
(Execution)-[:HAS_AGENT_RUN]->(AgentRun)
(AgentRun)-[:INSTANCE_OF]->(Agent)
(AgentRun)-[:HAS_TOOL_RUN]->(ToolRun)

(Session)-[:HAS_CONTACT]->(UserContact)
(UserContact)-[:REQUESTED_BY]->(AgentRun)
(UserContact)-[:ANSWERED_BY]->(Message)
```

### External Knowledge domain

```text
(Knowledge {id: "external_knowledge"})
    -[:HAS_DOCUMENT]->(KnowledgeDocument)

(KnowledgeDocument)-[:HAS_CHUNK]->(KnowledgeChunk)
(Knowledge)-[:HAS_ENTITY]->(KnowledgeEntity)

(KnowledgeChunk)-[:MENTIONS]->(KnowledgeEntity)
(KnowledgeEntity)-[:RELATED_TO]->(KnowledgeEntity)
(KnowledgeEntity)-[:SUPPORTED_BY]->(KnowledgeChunk)
```

The main Agent Space repository must ignore the external Knowledge domain during normal system discovery and execution queries.

Only explicit Knowledge agents access it.

Optional provenance links may connect the domains:

```text
(AgentRun)-[:IMPORTED]->(KnowledgeDocument)
(AgentRun)-[:RETRIEVED_FROM]->(KnowledgeEntity)
```

These links record provenance but do not make external knowledge part of the normal Agent Space control graph.

---

## 6. Required session statuses and events

```text
Session statuses:
    PENDING
    RUNNING
    WAITING_FOR_USER
    COMPLETED
    FAILED

Additional events:
    conversation_created
    message_received
    session_started
    analysis_started
    analysis_completed
    planning_started
    planning_completed
    agent_running
    tool_running
    user_contact_requested
    user_contact_answered
    session_resumed
    synthesis_started
    session_completed
    session_failed
```

A Session is completed only after its final response is saved and added to the Conversation.
